# Legacy Archive

`/home/ubuntu/quant-agent-platform` was the earlier bootstrap workspace for this project.

It is no longer an active deployment target. The useful reference material was archived under `docs/legacy/quant-agent-platform/` so the live product can stay consolidated in `/home/ubuntu/quant`.

Active development, Docker Compose runtime, and Git Flow now all belong to the `quant` repository only.
